// Generated by scripts/generate-android-platform-facts.ts; do not edit.
use serde::{Deserialize, Serialize};

pub(crate) const ANDROID_PLATFORM_FACTS_VERSION: u8 = 1;
pub(crate) const ANDROID_PLATFORM_FACTS_SAFE_INTEGER_MAX: u64 = 9007199254740991;
#[cfg(test)]
pub(crate) const ANDROID_PLATFORM_FACTS_GOLDEN_JSON: &str = r#"{"activationFailure":null,"activationSessionId":null,"activeNetwork":false,"configFailureInjectionAvailable":false,"coreAbiVersion":1,"coreAvailability":"available","coreCommit":"e26714a181ac0e2fa803453c0a8e9a9ce94e31cb","coreConfigState":"unloaded","coreRunning":false,"coreVersion":"v1.19.29","coreWrapperRevision":"mish-mobile-core-v1","dnsApplied":false,"event":"observation","factSequence":7,"factsVersion":1,"lifecycleAuthority":null,"loadedConfigDigest":null,"loadedConfigRevision":null,"notificationPermission":"required","observedAtMillis":1700000000000,"platformSessionId":"platform-session-1","protectedSocketCount":0,"publicRequestObserved":false,"recoveryEvidence":"none","routesApplied":false,"serviceForeground":false,"tunEstablished":false,"validatedConfigDigest":null,"validatedConfigRevision":null,"vpnPermission":"required"}"#;

#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
#[serde(rename_all = "kebab-case")]
pub(crate) enum PlatformEventKind {
    ActivationCompleted,
    ActivationFailed,
    ActivationProgress,
    ConsentResult,
    CoreExited,
    NetworkChanged,
    NotificationResult,
    Observation,
    Revoked,
    ServiceDestroyed,
    StopCompleted,
}

impl PlatformEventKind {
    pub(crate) const fn wire_name(self) -> &'static str {
        match self {
            Self::ActivationCompleted => "activation-completed",
            Self::ActivationFailed => "activation-failed",
            Self::ActivationProgress => "activation-progress",
            Self::ConsentResult => "consent-result",
            Self::CoreExited => "core-exited",
            Self::NetworkChanged => "network-changed",
            Self::NotificationResult => "notification-result",
            Self::Observation => "observation",
            Self::Revoked => "revoked",
            Self::ServiceDestroyed => "service-destroyed",
            Self::StopCompleted => "stop-completed",
        }
    }
}

impl From<&str> for PlatformEventKind {
    fn from(value: &str) -> Self {
        match value {
            "activation-completed" => Self::ActivationCompleted,
            "activation-failed" => Self::ActivationFailed,
            "activation-progress" => Self::ActivationProgress,
            "consent-result" => Self::ConsentResult,
            "core-exited" => Self::CoreExited,
            "network-changed" => Self::NetworkChanged,
            "notification-result" => Self::NotificationResult,
            "observation" => Self::Observation,
            "revoked" => Self::Revoked,
            "service-destroyed" => Self::ServiceDestroyed,
            "stop-completed" => Self::StopCompleted,
            _ => panic!("unknown PlatformEventKind wire value: {value}"),
        }
    }
}

impl PartialEq<&str> for PlatformEventKind {
    fn eq(&self, other: &&str) -> bool {
        self.wire_name() == *other
    }
}

#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
#[serde(rename_all = "kebab-case")]
pub(crate) enum PlatformFailureKind {
    CleanupFailed,
    ConfigurationNotLoaded,
    CoreExited,
    CoreStartFailed,
    CoreUnavailable,
    NetworkUnavailable,
    PermissionRevoked,
    PublicRequestFailed,
    TunEstablishFailed,
}

impl PlatformFailureKind {
    pub(crate) const fn wire_name(self) -> &'static str {
        match self {
            Self::CleanupFailed => "cleanup-failed",
            Self::ConfigurationNotLoaded => "configuration-not-loaded",
            Self::CoreExited => "core-exited",
            Self::CoreStartFailed => "core-start-failed",
            Self::CoreUnavailable => "core-unavailable",
            Self::NetworkUnavailable => "network-unavailable",
            Self::PermissionRevoked => "permission-revoked",
            Self::PublicRequestFailed => "public-request-failed",
            Self::TunEstablishFailed => "tun-establish-failed",
        }
    }
}

impl From<&str> for PlatformFailureKind {
    fn from(value: &str) -> Self {
        match value {
            "cleanup-failed" => Self::CleanupFailed,
            "configuration-not-loaded" => Self::ConfigurationNotLoaded,
            "core-exited" => Self::CoreExited,
            "core-start-failed" => Self::CoreStartFailed,
            "core-unavailable" => Self::CoreUnavailable,
            "network-unavailable" => Self::NetworkUnavailable,
            "permission-revoked" => Self::PermissionRevoked,
            "public-request-failed" => Self::PublicRequestFailed,
            "tun-establish-failed" => Self::TunEstablishFailed,
            _ => panic!("unknown PlatformFailureKind wire value: {value}"),
        }
    }
}

impl PartialEq<&str> for PlatformFailureKind {
    fn eq(&self, other: &&str) -> bool {
        self.wire_name() == *other
    }
}

#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
#[serde(rename_all = "kebab-case")]
pub(crate) enum PlatformRecoveryEvidence {
    ForegroundExpected,
    Invalid,
    None,
}

impl PlatformRecoveryEvidence {
    pub(crate) const fn wire_name(self) -> &'static str {
        match self {
            Self::ForegroundExpected => "foreground-expected",
            Self::Invalid => "invalid",
            Self::None => "none",
        }
    }
}

impl From<&str> for PlatformRecoveryEvidence {
    fn from(value: &str) -> Self {
        match value {
            "foreground-expected" => Self::ForegroundExpected,
            "invalid" => Self::Invalid,
            "none" => Self::None,
            _ => panic!("unknown PlatformRecoveryEvidence wire value: {value}"),
        }
    }
}

impl PartialEq<&str> for PlatformRecoveryEvidence {
    fn eq(&self, other: &&str) -> bool {
        self.wire_name() == *other
    }
}

#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
#[serde(rename_all = "kebab-case")]
pub(crate) enum VpnPermission {
    Granted,
    Required,
    Unknown,
}

impl VpnPermission {
    pub(crate) const fn wire_name(self) -> &'static str {
        match self {
            Self::Granted => "granted",
            Self::Required => "required",
            Self::Unknown => "unknown",
        }
    }
}

impl From<&str> for VpnPermission {
    fn from(value: &str) -> Self {
        match value {
            "granted" => Self::Granted,
            "required" => Self::Required,
            "unknown" => Self::Unknown,
            _ => panic!("unknown VpnPermission wire value: {value}"),
        }
    }
}

impl PartialEq<&str> for VpnPermission {
    fn eq(&self, other: &&str) -> bool {
        self.wire_name() == *other
    }
}

#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
#[serde(rename_all = "kebab-case")]
pub(crate) enum NotificationPermission {
    Denied,
    Granted,
    NotRequired,
    Required,
}

impl NotificationPermission {
    pub(crate) const fn wire_name(self) -> &'static str {
        match self {
            Self::Denied => "denied",
            Self::Granted => "granted",
            Self::NotRequired => "not-required",
            Self::Required => "required",
        }
    }
}

impl From<&str> for NotificationPermission {
    fn from(value: &str) -> Self {
        match value {
            "denied" => Self::Denied,
            "granted" => Self::Granted,
            "not-required" => Self::NotRequired,
            "required" => Self::Required,
            _ => panic!("unknown NotificationPermission wire value: {value}"),
        }
    }
}

impl PartialEq<&str> for NotificationPermission {
    fn eq(&self, other: &&str) -> bool {
        self.wire_name() == *other
    }
}

#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
#[serde(rename_all = "kebab-case")]
pub(crate) enum PlatformAvailability {
    Available,
    Unavailable,
}

impl PlatformAvailability {
    pub(crate) const fn wire_name(self) -> &'static str {
        match self {
            Self::Available => "available",
            Self::Unavailable => "unavailable",
        }
    }
}

impl From<&str> for PlatformAvailability {
    fn from(value: &str) -> Self {
        match value {
            "available" => Self::Available,
            "unavailable" => Self::Unavailable,
            _ => panic!("unknown PlatformAvailability wire value: {value}"),
        }
    }
}

impl PartialEq<&str> for PlatformAvailability {
    fn eq(&self, other: &&str) -> bool {
        self.wire_name() == *other
    }
}

#[derive(Clone, Copy, Debug, Deserialize, Eq, PartialEq, Serialize)]
#[serde(rename_all = "kebab-case")]
pub(crate) enum CoreConfigState {
    Loaded,
    Unknown,
    Unloaded,
}

impl CoreConfigState {
    pub(crate) const fn wire_name(self) -> &'static str {
        match self {
            Self::Loaded => "loaded",
            Self::Unknown => "unknown",
            Self::Unloaded => "unloaded",
        }
    }
}

impl From<&str> for CoreConfigState {
    fn from(value: &str) -> Self {
        match value {
            "loaded" => Self::Loaded,
            "unknown" => Self::Unknown,
            "unloaded" => Self::Unloaded,
            _ => panic!("unknown CoreConfigState wire value: {value}"),
        }
    }
}

impl PartialEq<&str> for CoreConfigState {
    fn eq(&self, other: &&str) -> bool {
        self.wire_name() == *other
    }
}

#[derive(Clone, Debug, Deserialize, Eq, PartialEq, Serialize)]
#[serde(rename_all = "camelCase", deny_unknown_fields)]
pub(crate) struct PlatformLifecycleAuthority {
    pub machine_authority: String,
    pub scope_epoch: u64,
    pub operation_id: String,
    pub admitted_revision: u64,
    pub effect_identity: String,
}

impl PlatformLifecycleAuthority {
    pub(crate) fn valid(&self) -> bool {
        valid_identifier(&self.machine_authority)
            && (1..=ANDROID_PLATFORM_FACTS_SAFE_INTEGER_MAX).contains(&self.scope_epoch)
            && valid_identifier(&self.operation_id)
            && (1..=ANDROID_PLATFORM_FACTS_SAFE_INTEGER_MAX).contains(&self.admitted_revision)
            && self
                .effect_identity
                .parse::<u64>()
                .is_ok_and(|effect| (1..=ANDROID_PLATFORM_FACTS_SAFE_INTEGER_MAX).contains(&effect))
    }
}

#[derive(Clone, Debug, Deserialize, Eq, PartialEq, Serialize)]
#[serde(rename_all = "camelCase", deny_unknown_fields)]
pub(crate) struct PlatformFacts {
    #[serde(deserialize_with = "deserialize_required_option")]
    pub activation_failure: Option<PlatformFailureKind>,
    #[serde(deserialize_with = "deserialize_required_option")]
    pub activation_session_id: Option<String>,
    pub active_network: bool,
    pub config_failure_injection_available: bool,
    #[serde(deserialize_with = "deserialize_required_option")]
    pub core_abi_version: Option<u8>,
    pub core_availability: PlatformAvailability,
    #[serde(deserialize_with = "deserialize_required_option")]
    pub core_commit: Option<String>,
    pub core_config_state: CoreConfigState,
    pub core_running: bool,
    #[serde(deserialize_with = "deserialize_required_option")]
    pub core_version: Option<String>,
    #[serde(deserialize_with = "deserialize_required_option")]
    pub core_wrapper_revision: Option<String>,
    pub dns_applied: bool,
    pub event: PlatformEventKind,
    pub fact_sequence: u64,
    pub facts_version: u8,
    #[serde(deserialize_with = "deserialize_required_option")]
    pub lifecycle_authority: Option<PlatformLifecycleAuthority>,
    #[serde(deserialize_with = "deserialize_required_option")]
    pub loaded_config_digest: Option<String>,
    #[serde(deserialize_with = "deserialize_required_option")]
    pub loaded_config_revision: Option<String>,
    pub notification_permission: NotificationPermission,
    pub observed_at_millis: u64,
    pub platform_session_id: String,
    pub protected_socket_count: u64,
    pub public_request_observed: bool,
    pub recovery_evidence: PlatformRecoveryEvidence,
    pub routes_applied: bool,
    pub service_foreground: bool,
    pub tun_established: bool,
    #[serde(deserialize_with = "deserialize_required_option")]
    pub validated_config_digest: Option<String>,
    #[serde(deserialize_with = "deserialize_required_option")]
    pub validated_config_revision: Option<String>,
    pub vpn_permission: VpnPermission,
}

fn deserialize_required_option<'de, D, T>(deserializer: D) -> Result<Option<T>, D::Error>
where
    D: serde::Deserializer<'de>,
    T: Deserialize<'de>,
{
    Option::<T>::deserialize(deserializer)
}

impl PlatformFacts {
    pub(crate) fn validate(&self) -> Result<(), &'static str> {
        if self.facts_version != ANDROID_PLATFORM_FACTS_VERSION {
            return Err("unsupported factsVersion");
        }
        if !(1..=ANDROID_PLATFORM_FACTS_SAFE_INTEGER_MAX).contains(&self.fact_sequence) {
            return Err("factSequence is outside the wire integer bound");
        }
        if self.observed_at_millis > ANDROID_PLATFORM_FACTS_SAFE_INTEGER_MAX {
            return Err("observedAtMillis is outside the wire integer bound");
        }
        if !valid_identifier(&self.platform_session_id)
            || !valid_optional_identifier(&self.activation_session_id, 128)
            || !valid_optional_identifier(&self.loaded_config_revision, 128)
            || !valid_optional_identifier(&self.validated_config_revision, 128)
        {
            return Err("platform fact identity is invalid");
        }
        if self.protected_socket_count > 1 {
            return Err("protectedSocketCount exceeds its bound");
        }
        if self.core_abi_version.is_some_and(|version| version != 1)
            || !valid_optional_core_commit(&self.core_commit)
            || !valid_optional_wire_text(&self.core_version, 32)
            || !valid_optional_identifier(&self.core_wrapper_revision, 64)
        {
            return Err("Mobile Core identity is invalid");
        }
        let core_identity_complete = self.core_abi_version.is_some()
            && self.core_commit.is_some()
            && self.core_version.is_some()
            && self.core_wrapper_revision.is_some();
        if (self.core_availability == PlatformAvailability::Available) != core_identity_complete {
            return Err("Mobile Core availability and identity disagree");
        }
        if !valid_optional_digest(&self.loaded_config_digest)
            || !valid_optional_digest(&self.validated_config_digest)
        {
            return Err("configuration digest is invalid");
        }
        let loaded_complete =
            self.loaded_config_digest.is_some() && self.loaded_config_revision.is_some();
        if (self.core_config_state == CoreConfigState::Loaded) != loaded_complete {
            return Err("loaded configuration identity is inconsistent");
        }
        if self.validated_config_digest.is_some() != self.validated_config_revision.is_some() {
            return Err("validated configuration identity is incomplete");
        }
        if self
            .lifecycle_authority
            .as_ref()
            .is_some_and(|authority| !authority.valid())
        {
            return Err("lifecycleAuthority is invalid");
        }
        Ok(())
    }
}

fn valid_identifier(value: &str) -> bool {
    !value.is_empty()
        && value.len() <= 128
        && value
            .bytes()
            .all(|byte| byte.is_ascii_alphanumeric() || matches!(byte, b'-' | b'_' | b'.'))
}

fn valid_optional_identifier(value: &Option<String>, max: usize) -> bool {
    value
        .as_ref()
        .is_none_or(|value| !value.is_empty() && value.len() <= max && valid_identifier(value))
}

fn valid_optional_core_commit(value: &Option<String>) -> bool {
    value.as_ref().is_none_or(|value| {
        (7..=64).contains(&value.len())
            && value
                .bytes()
                .all(|byte| byte.is_ascii_hexdigit() && !byte.is_ascii_uppercase())
    })
}

fn valid_optional_wire_text(value: &Option<String>, max: usize) -> bool {
    value.as_ref().is_none_or(|value| {
        !value.is_empty()
            && value.len() <= max
            && value.bytes().all(|byte| {
                byte.is_ascii_alphanumeric() || matches!(byte, b'-' | b'_' | b'.' | b'+')
            })
    })
}

fn valid_optional_digest(value: &Option<String>) -> bool {
    value.as_ref().is_none_or(|value| {
        value.len() == 64
            && value
                .bytes()
                .all(|byte| byte.is_ascii_hexdigit() && !byte.is_ascii_uppercase())
    })
}
