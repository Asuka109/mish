{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allows Mish bootstrap and native title-bar gestures in the main local WebView.",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "allow-runtime-bootstrap",
    "core:window:allow-start-dragging",
    "core:window:allow-toggle-maximize"
  ]
}
