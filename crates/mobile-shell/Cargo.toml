[package]
name = "mish-mobile-shell"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Production-disabled Shared Rust authority for installed-mobile shell entry"

[dev-dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
