ipv6: false
dns:
  enable: false
proxies:
  - name: fixture-direct
    type: direct
    udp: true
proxy-groups:
  - name: Fixture route
    type: select
    proxies:
      - fixture-direct
      - DIRECT
      - REJECT
rules:
  - DOMAIN-SUFFIX,fixture.invalid,Fixture route
  - MATCH,DIRECT
