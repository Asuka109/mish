// Generated by scripts/generate-presentation-contract.ts; do not edit.
import * as z from "zod";

export const presentationLocales = ["en", "zh-CN"] as const;
export type PresentationLocale = (typeof presentationLocales)[number];
export const nativeMessageIds = [
  "status.open-mish",
  "status.open-browser",
  "status.open-routes",
  "status.open-profiles",
  "status.open-traffic",
  "status.open-events",
  "status.open-settings",
  "status.launch-proxy",
  "status.launch-proxy-pending",
  "status.launch-proxy-failed",
  "status.stop-proxy",
  "status.auto-start-proxy",
  "status.quit",
  "status.live.idle",
  "status.live.unavailable",
  "status.live.most-active-node",
  "status.live.rate",
  "application.settings",
  "application.find",
  "application.quit",
] as const;
export type NativeMessageId = (typeof nativeMessageIds)[number];
export const nativeActionIds = [
  "application.find",
  "application.quit",
  "application.settings",
  "status-bar.open-browser",
  "status-bar.open-events",
  "status-bar.open-mish",
  "status-bar.open-profiles",
  "status-bar.open-routes",
  "status-bar.open-settings",
  "status-bar.open-traffic",
  "status-bar.quit",
  "status-bar.toggle-launch-on-start",
  "status-bar.toggle-proxy",
] as const;
export type NativeActionId = (typeof nativeActionIds)[number];
export const applicationActionIds = [
  "find-ports-and-retry",
  "leave-as-is",
  "open-system-proxy-settings",
  "open-welcome",
  "repair",
  "retry-profile-activation",
  "show-system-proxy-settings-steps",
] as const;
export const applicationActionIdSchema = z.enum(applicationActionIds);
export type ApplicationActionId = z.infer<typeof applicationActionIdSchema>;

export type NativeMessageArgs = {
  "status.open-mish": {};
  "status.open-browser": {};
  "status.open-routes": {};
  "status.open-profiles": {};
  "status.open-traffic": {};
  "status.open-events": {};
  "status.open-settings": {};
  "status.launch-proxy": {};
  "status.launch-proxy-pending": {};
  "status.launch-proxy-failed": {};
  "status.stop-proxy": {};
  "status.auto-start-proxy": {};
  "status.quit": {};
  "status.live.idle": {};
  "status.live.unavailable": {};
  "status.live.most-active-node": { label: string };
  "status.live.rate": { direction: string; rate: string; total: string };
  "application.settings": {};
  "application.find": {};
  "application.quit": {};
};

export type NativeMessage =
  | { id: "status.open-mish" }
  | { id: "status.open-browser" }
  | { id: "status.open-routes" }
  | { id: "status.open-profiles" }
  | { id: "status.open-traffic" }
  | { id: "status.open-events" }
  | { id: "status.open-settings" }
  | { id: "status.launch-proxy" }
  | { id: "status.launch-proxy-pending" }
  | { id: "status.launch-proxy-failed" }
  | { id: "status.stop-proxy" }
  | { id: "status.auto-start-proxy" }
  | { id: "status.quit" }
  | { id: "status.live.idle" }
  | { id: "status.live.unavailable" }
  | { id: "status.live.most-active-node"; label: string }
  | { id: "status.live.rate"; direction: string; rate: string; total: string }
  | { id: "application.settings" }
  | { id: "application.find" }
  | { id: "application.quit" };

const applicationNotificationCaptureFailureDataSchema = z
  .object({
    failure: z.string(),
    observationStage: z.string().optional(),
    takeoverReason: z.string().optional(),
  })
  .strict();

const applicationNotificationLocalProxyFeedbackDataSchema = z
  .object({
    outcome: z.string(),
  })
  .strict();

const applicationNotificationOnboardingWelcomeDataSchema = z
  .object({
    prompt: z.boolean(),
  })
  .strict();

const applicationNotificationProfileActivationAsnFailedDataSchema = z
  .object({
    asset: z.string(),
    outcome: z.string(),
  })
  .strict();

const applicationNotificationProfileActivationAsnProgressDataSchema = z
  .object({
    asset: z.string(),
  })
  .strict();

const applicationNotificationProfileActivationFailedDataSchema = z
  .object({
    failure: z.string(),
  })
  .strict();

const applicationNotificationProfileActivationGeoipFailedDataSchema = z
  .object({
    asset: z.string(),
    outcome: z.string(),
  })
  .strict();

const applicationNotificationProfileActivationGeoipProgressDataSchema = z
  .object({
    asset: z.string(),
  })
  .strict();

const applicationNotificationProfileActivationGeositeFailedDataSchema = z
  .object({
    asset: z.string(),
    outcome: z.string(),
  })
  .strict();

const applicationNotificationProfileActivationGeositeProgressDataSchema = z
  .object({
    asset: z.string(),
  })
  .strict();

const applicationNotificationProfileActivationListenerConflictDataSchema = z
  .object({
    endpoint: z.string(),
  })
  .strict();

const applicationNotificationProfileActivationMmdbFailedDataSchema = z
  .object({
    asset: z.string(),
    outcome: z.string(),
  })
  .strict();

const applicationNotificationProfileActivationMmdbProgressDataSchema = z
  .object({
    asset: z.string(),
  })
  .strict();

const applicationNotificationProfileCreateFailedDataSchema = z.object({}).strict();

const applicationNotificationProfileCreatedDataSchema = z.object({}).strict();

const applicationNotificationProfileDetachFailedDataSchema = z.object({}).strict();

const applicationNotificationProfileDetachedDataSchema = z.object({}).strict();

const applicationNotificationProfileFileActionFailedDataSchema = z.object({}).strict();

const applicationNotificationProfileImportFailedDataSchema = z.object({}).strict();

const applicationNotificationProfilePatchLoadFailedDataSchema = z.object({}).strict();

const applicationNotificationProfilePatchSaveFailedDataSchema = z.object({}).strict();

const applicationNotificationProfilePatchSavedDataSchema = z.object({}).strict();

const applicationNotificationProfileRefreshFailedDataSchema = z.object({}).strict();

const applicationNotificationProfileSaveFailedDataSchema = z.object({}).strict();

const applicationNotificationProfileSavedDataSchema = z.object({}).strict();

const applicationNotificationProfileScheduleFailedDataSchema = z.object({}).strict();

const applicationNotificationProfileSubscriptionUpdatedDataSchema = z.object({}).strict();

const applicationNotificationProfileSwitchFailedDataSchema = z.object({}).strict();

const applicationNotificationRouteSelectionFailedDataSchema = z
  .object({
    child: z.string(),
  })
  .strict();

const applicationNotificationServiceDefaultsRestoredDataSchema = z.object({}).strict();

const applicationNotificationServiceRemovedDataSchema = z.object({}).strict();

const applicationNotificationServiceSavedDataSchema = z
  .object({
    operation: z.string(),
  })
  .strict();

const applicationNotificationSettingsOperationFailedDataSchema = z
  .object({
    failure: z.string(),
  })
  .strict();

const applicationNotificationStatusOperationFailedDataSchema = z
  .object({
    failure: z.string().optional(),
  })
  .strict();

const applicationNotificationSystemProxyDriftDataSchema = z
  .object({
    canLeave: z.boolean(),
    canRepair: z.boolean(),
    failure: z.string().optional(),
    repairRequiresCore: z.boolean(),
  })
  .strict();

const applicationNotificationSystemProxyFailedDataSchema = z
  .object({
    failure: z.string().optional(),
  })
  .strict();

const applicationNotificationTrafficConnectionClosedDataSchema = z.object({}).strict();

const applicationNotificationTrafficConnectionsClosedDataSchema = z
  .object({
    count: z.number(),
  })
  .strict();

const applicationNotificationTrafficOperationFailedDataSchema = z
  .object({
    failure: z.string(),
  })
  .strict();

const applicationNotificationTunDriftDataSchema = z.object({}).strict();

const applicationNotificationTunFailedDataSchema = z.object({}).strict();

export const applicationNotificationKindSchema = z.enum([
  "capture.failure",
  "local-proxy.feedback",
  "onboarding.welcome",
  "profile.activation-asn-failed",
  "profile.activation-asn-progress",
  "profile.activation-failed",
  "profile.activation-geoip-failed",
  "profile.activation-geoip-progress",
  "profile.activation-geosite-failed",
  "profile.activation-geosite-progress",
  "profile.activation-listener-conflict",
  "profile.activation-mmdb-failed",
  "profile.activation-mmdb-progress",
  "profile.create-failed",
  "profile.created",
  "profile.detach-failed",
  "profile.detached",
  "profile.file-action-failed",
  "profile.import-failed",
  "profile.patch-load-failed",
  "profile.patch-save-failed",
  "profile.patch-saved",
  "profile.refresh-failed",
  "profile.save-failed",
  "profile.saved",
  "profile.schedule-failed",
  "profile.subscription-updated",
  "profile.switch-failed",
  "route.selection-failed",
  "service.defaults-restored",
  "service.removed",
  "service.saved",
  "settings.operation-failed",
  "status.operation-failed",
  "system-proxy.drift",
  "system-proxy.failed",
  "traffic.connection-closed",
  "traffic.connections-closed",
  "traffic.operation-failed",
  "tun.drift",
  "tun.failed",
]);
export type ApplicationNotificationKind = z.infer<typeof applicationNotificationKindSchema>;
export const applicationNotificationSchema = z.discriminatedUnion("kind", [
  z
    .object({
      actionIds: z
        .array(
          z.enum([
            "leave-as-is",
            "open-system-proxy-settings",
            "repair",
            "show-system-proxy-settings-steps",
          ]),
        )
        .max(4)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationCaptureFailureDataSchema,
      kind: z.literal("capture.failure"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationLocalProxyFeedbackDataSchema,
      kind: z.literal("local-proxy.feedback"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.enum(["open-welcome"]))
        .max(1)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationOnboardingWelcomeDataSchema,
      kind: z.literal("onboarding.welcome"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileActivationAsnFailedDataSchema,
      kind: z.literal("profile.activation-asn-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileActivationAsnProgressDataSchema,
      kind: z.literal("profile.activation-asn-progress"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.enum(["retry-profile-activation"]))
        .max(1)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileActivationFailedDataSchema,
      kind: z.literal("profile.activation-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileActivationGeoipFailedDataSchema,
      kind: z.literal("profile.activation-geoip-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileActivationGeoipProgressDataSchema,
      kind: z.literal("profile.activation-geoip-progress"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileActivationGeositeFailedDataSchema,
      kind: z.literal("profile.activation-geosite-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileActivationGeositeProgressDataSchema,
      kind: z.literal("profile.activation-geosite-progress"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.enum(["find-ports-and-retry"]))
        .max(1)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileActivationListenerConflictDataSchema,
      kind: z.literal("profile.activation-listener-conflict"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileActivationMmdbFailedDataSchema,
      kind: z.literal("profile.activation-mmdb-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileActivationMmdbProgressDataSchema,
      kind: z.literal("profile.activation-mmdb-progress"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileCreateFailedDataSchema,
      kind: z.literal("profile.create-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileCreatedDataSchema,
      kind: z.literal("profile.created"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileDetachFailedDataSchema,
      kind: z.literal("profile.detach-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileDetachedDataSchema,
      kind: z.literal("profile.detached"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileFileActionFailedDataSchema,
      kind: z.literal("profile.file-action-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileImportFailedDataSchema,
      kind: z.literal("profile.import-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfilePatchLoadFailedDataSchema,
      kind: z.literal("profile.patch-load-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfilePatchSaveFailedDataSchema,
      kind: z.literal("profile.patch-save-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfilePatchSavedDataSchema,
      kind: z.literal("profile.patch-saved"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileRefreshFailedDataSchema,
      kind: z.literal("profile.refresh-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileSaveFailedDataSchema,
      kind: z.literal("profile.save-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileSavedDataSchema,
      kind: z.literal("profile.saved"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileScheduleFailedDataSchema,
      kind: z.literal("profile.schedule-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileSubscriptionUpdatedDataSchema,
      kind: z.literal("profile.subscription-updated"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationProfileSwitchFailedDataSchema,
      kind: z.literal("profile.switch-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationRouteSelectionFailedDataSchema,
      kind: z.literal("route.selection-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationServiceDefaultsRestoredDataSchema,
      kind: z.literal("service.defaults-restored"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationServiceRemovedDataSchema,
      kind: z.literal("service.removed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationServiceSavedDataSchema,
      kind: z.literal("service.saved"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationSettingsOperationFailedDataSchema,
      kind: z.literal("settings.operation-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationStatusOperationFailedDataSchema,
      kind: z.literal("status.operation-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.enum(["leave-as-is", "repair"]))
        .max(2)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationSystemProxyDriftDataSchema,
      kind: z.literal("system-proxy.drift"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationSystemProxyFailedDataSchema,
      kind: z.literal("system-proxy.failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationTrafficConnectionClosedDataSchema,
      kind: z.literal("traffic.connection-closed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationTrafficConnectionsClosedDataSchema,
      kind: z.literal("traffic.connections-closed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationTrafficOperationFailedDataSchema,
      kind: z.literal("traffic.operation-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationTunDriftDataSchema,
      kind: z.literal("tun.drift"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationNotificationTunFailedDataSchema,
      kind: z.literal("tun.failed"),
    })
    .strict(),
]);
export type ApplicationNotification = z.infer<typeof applicationNotificationSchema>;
export interface ApplicationNotificationDataByKind {
  "capture.failure": z.infer<typeof applicationNotificationCaptureFailureDataSchema>;
  "local-proxy.feedback": z.infer<typeof applicationNotificationLocalProxyFeedbackDataSchema>;
  "onboarding.welcome": z.infer<typeof applicationNotificationOnboardingWelcomeDataSchema>;
  "profile.activation-asn-failed": z.infer<
    typeof applicationNotificationProfileActivationAsnFailedDataSchema
  >;
  "profile.activation-asn-progress": z.infer<
    typeof applicationNotificationProfileActivationAsnProgressDataSchema
  >;
  "profile.activation-failed": z.infer<
    typeof applicationNotificationProfileActivationFailedDataSchema
  >;
  "profile.activation-geoip-failed": z.infer<
    typeof applicationNotificationProfileActivationGeoipFailedDataSchema
  >;
  "profile.activation-geoip-progress": z.infer<
    typeof applicationNotificationProfileActivationGeoipProgressDataSchema
  >;
  "profile.activation-geosite-failed": z.infer<
    typeof applicationNotificationProfileActivationGeositeFailedDataSchema
  >;
  "profile.activation-geosite-progress": z.infer<
    typeof applicationNotificationProfileActivationGeositeProgressDataSchema
  >;
  "profile.activation-listener-conflict": z.infer<
    typeof applicationNotificationProfileActivationListenerConflictDataSchema
  >;
  "profile.activation-mmdb-failed": z.infer<
    typeof applicationNotificationProfileActivationMmdbFailedDataSchema
  >;
  "profile.activation-mmdb-progress": z.infer<
    typeof applicationNotificationProfileActivationMmdbProgressDataSchema
  >;
  "profile.create-failed": z.infer<typeof applicationNotificationProfileCreateFailedDataSchema>;
  "profile.created": z.infer<typeof applicationNotificationProfileCreatedDataSchema>;
  "profile.detach-failed": z.infer<typeof applicationNotificationProfileDetachFailedDataSchema>;
  "profile.detached": z.infer<typeof applicationNotificationProfileDetachedDataSchema>;
  "profile.file-action-failed": z.infer<
    typeof applicationNotificationProfileFileActionFailedDataSchema
  >;
  "profile.import-failed": z.infer<typeof applicationNotificationProfileImportFailedDataSchema>;
  "profile.patch-load-failed": z.infer<
    typeof applicationNotificationProfilePatchLoadFailedDataSchema
  >;
  "profile.patch-save-failed": z.infer<
    typeof applicationNotificationProfilePatchSaveFailedDataSchema
  >;
  "profile.patch-saved": z.infer<typeof applicationNotificationProfilePatchSavedDataSchema>;
  "profile.refresh-failed": z.infer<typeof applicationNotificationProfileRefreshFailedDataSchema>;
  "profile.save-failed": z.infer<typeof applicationNotificationProfileSaveFailedDataSchema>;
  "profile.saved": z.infer<typeof applicationNotificationProfileSavedDataSchema>;
  "profile.schedule-failed": z.infer<typeof applicationNotificationProfileScheduleFailedDataSchema>;
  "profile.subscription-updated": z.infer<
    typeof applicationNotificationProfileSubscriptionUpdatedDataSchema
  >;
  "profile.switch-failed": z.infer<typeof applicationNotificationProfileSwitchFailedDataSchema>;
  "route.selection-failed": z.infer<typeof applicationNotificationRouteSelectionFailedDataSchema>;
  "service.defaults-restored": z.infer<
    typeof applicationNotificationServiceDefaultsRestoredDataSchema
  >;
  "service.removed": z.infer<typeof applicationNotificationServiceRemovedDataSchema>;
  "service.saved": z.infer<typeof applicationNotificationServiceSavedDataSchema>;
  "settings.operation-failed": z.infer<
    typeof applicationNotificationSettingsOperationFailedDataSchema
  >;
  "status.operation-failed": z.infer<typeof applicationNotificationStatusOperationFailedDataSchema>;
  "system-proxy.drift": z.infer<typeof applicationNotificationSystemProxyDriftDataSchema>;
  "system-proxy.failed": z.infer<typeof applicationNotificationSystemProxyFailedDataSchema>;
  "traffic.connection-closed": z.infer<
    typeof applicationNotificationTrafficConnectionClosedDataSchema
  >;
  "traffic.connections-closed": z.infer<
    typeof applicationNotificationTrafficConnectionsClosedDataSchema
  >;
  "traffic.operation-failed": z.infer<
    typeof applicationNotificationTrafficOperationFailedDataSchema
  >;
  "tun.drift": z.infer<typeof applicationNotificationTunDriftDataSchema>;
  "tun.failed": z.infer<typeof applicationNotificationTunFailedDataSchema>;
}

const applicationEventCaptureFailureDataSchema = z
  .object({
    failure: z.string(),
    observationStage: z.string().optional(),
  })
  .strict();

const applicationEventControllerSessionStartedDataSchema = z.object({}).strict();

const applicationEventControllerSessionStaleDataSchema = z.object({}).strict();

const applicationEventControllerStreamUnavailableDataSchema = z
  .object({
    failure: z.string(),
  })
  .strict();

const applicationEventProfileActivationFailedDataSchema = z
  .object({
    failure: z.string(),
  })
  .strict();

const applicationEventProxyLaunchTimingDataSchema = z
  .object({
    listenerJournalMutationConfirmationMs: z.number(),
    outcome: z.string(),
    overlapMs: z.number(),
    preparationWallMs: z.number(),
    profileCoreMs: z.number(),
    schemaVersion: z.number(),
    systemProxyPreflightMs: z.number(),
    totalMs: z.number(),
  })
  .strict();

const applicationEventSettingsOperationFailedDataSchema = z
  .object({
    failure: z.string(),
  })
  .strict();

const applicationEventTrafficOperationFailedDataSchema = z
  .object({
    failure: z.string(),
  })
  .strict();

export const applicationEventKindSchema = z.enum([
  "capture.failure",
  "controller.session-started",
  "controller.session-stale",
  "controller.stream-unavailable",
  "profile.activation-failed",
  "proxy.launch-timing",
  "settings.operation-failed",
  "traffic.operation-failed",
]);
export type ApplicationEventKind = z.infer<typeof applicationEventKindSchema>;
export const applicationEventSchema = z.discriminatedUnion("kind", [
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationEventCaptureFailureDataSchema,
      kind: z.literal("capture.failure"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationEventControllerSessionStartedDataSchema,
      kind: z.literal("controller.session-started"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationEventControllerSessionStaleDataSchema,
      kind: z.literal("controller.session-stale"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationEventControllerStreamUnavailableDataSchema,
      kind: z.literal("controller.stream-unavailable"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationEventProfileActivationFailedDataSchema,
      kind: z.literal("profile.activation-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationEventProxyLaunchTimingDataSchema,
      kind: z.literal("proxy.launch-timing"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationEventSettingsOperationFailedDataSchema,
      kind: z.literal("settings.operation-failed"),
    })
    .strict(),
  z
    .object({
      actionIds: z
        .array(z.never())
        .max(0)
        .refine((ids) => new Set(ids).size === ids.length, "Action IDs must be unique"),
      data: applicationEventTrafficOperationFailedDataSchema,
      kind: z.literal("traffic.operation-failed"),
    })
    .strict(),
]);
export type ApplicationEvent = z.infer<typeof applicationEventSchema>;
export interface ApplicationEventDataByKind {
  "capture.failure": z.infer<typeof applicationEventCaptureFailureDataSchema>;
  "controller.session-started": z.infer<typeof applicationEventControllerSessionStartedDataSchema>;
  "controller.session-stale": z.infer<typeof applicationEventControllerSessionStaleDataSchema>;
  "controller.stream-unavailable": z.infer<
    typeof applicationEventControllerStreamUnavailableDataSchema
  >;
  "profile.activation-failed": z.infer<typeof applicationEventProfileActivationFailedDataSchema>;
  "proxy.launch-timing": z.infer<typeof applicationEventProxyLaunchTimingDataSchema>;
  "settings.operation-failed": z.infer<typeof applicationEventSettingsOperationFailedDataSchema>;
  "traffic.operation-failed": z.infer<typeof applicationEventTrafficOperationFailedDataSchema>;
}
